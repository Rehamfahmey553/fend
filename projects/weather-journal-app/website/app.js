/* Global Variables */
//create3requests;
//GET REQUEST=>OPEN WEATHEr MAP API
constGetData=async()=>{try{constrequest=awaitgetch(: ")//jsonformat
                                                   constresponse=awaitrequest.Josn()
                           consttemp=response.main.tempreturn temp{ catch(err){console.log(err)}}
                           // 2postrequest=>localserver
                           const server = app.listen(port, ()=>{console.log(`running on localhost: ${port}`)})
                           function listening(){
     console.log("server running"); 
     console.log(`running on localhost: {$port}`);
}
                           

// Create a new date instance dynamically with JS
let d = new Date();
let newDate = d.getMonth()+'.'+ d.getDate()+'.'+ d.getFullYear();
